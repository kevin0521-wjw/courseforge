# 参与开发 CourseForge

感谢你有兴趣参与课表工坊。这是一个**单人维护**的开源项目，任何人都可以提 issue、发 PR，
但请先读完以下约定 —— 它们不是形式条款，而是这个项目能保持"零依赖 + 高测试覆盖"的原因。

## 项目铁律（PR 会被拒绝的情况）

这四条是硬性要求，破坏其中任何一条的 PR 都不会被合并：

1. **零外部依赖。**
   不引 CDN、不引框架、不引字体库、不引 UI 库。交付产物（网页版 / 单文件版 / 桌面端）
   不允许依赖任何 npm 包。新增 npm 依赖只允许出现在**测试层**（`devDependencies`），
   且必须是"未安装时自动跳过、不阻断"的形式。
   界面图标全部内联 SVG；唯一的位图是应用图标，因为它必须满足平台硬要求
   （iOS 的 `apple-touch-icon` 与 Windows 的 `.ico` 都不接受 SVG）。

2. **数据默认留在本地。**
   不允许引入任何会把用户课表、账号、作息数据发送到第三方服务器的功能。
   云同步只允许「用户自备 WebDAV + 手动触发」这一种形态。

3. **渲染用户数据必须转义。**
   所有写入 DOM 的用户数据都要经过 `esc()`，包括课名、教师、地点这些看起来"安全"的字段。
   相关用例已在测试里钉死，绕过它 CI 会红。

4. **移动端硬指标不能降。**
   按钮点击区 ≥ 44px，输入框字号 ≥ 16px。这是可用性底线，不是建议。

## 代码约定

- **注释与文档使用中文。**
- **新增纯逻辑优先放进 `web/js/core.js`**，并同步补测试。
  渲染层、事件绑定、`localStorage` 读写都不适合放 `core.js`。
- **改动必须落盘。** 任何状态变更（设置、学期、课程）都要立即持久化，
  不能只改内存对象等下次保存 —— 项目里有专门的护栏测试盯着这件事。
- **不要为了通过测试而改测试。** 如果测试挂了，先确认是实现错了还是断言错了；
  断言确实过时时，在 PR 描述里说明理由。

## 开发流程

```bash
# 1. 克隆
git clone https://github.com/kevin0521-wjw/courseforge.git
cd courseforge

# 2. 跑测试（不需要 npm install 也能跑大部分用例）
npm test

# 3. 完整校验（含 DOM 检查、变异测试、单文件版构建与产物校验）
npm run verify

# 4. 本地起网页版
npm run start:web        # http://localhost:5173
```

**提交前请至少跑一遍 `npm test`。** 若改动涉及 DOM 结构、图标、打包产物，
再跑 `npm run verify` —— 它比测试更严格，且会真的构建一遍单文件版并扫描残留的外部引用。

### 关于测试与变异测试

项目当前有 **598 项** `node --test` 用例，全部通过是合并的前提。

此外还有变异测试（`npm run check:mutation`，89 条变异）：它会主动删掉源码里的某段逻辑，
检查是否有测试因此变红。如果删掉代码后测试仍然全绿，说明这段代码**没有被守护**
（术语：假护栏）。发现假护栏时必须二选一：

- 补一条能抓住它的断言；或
- 在变异登记表里显式登记为**等价变异**并写清理由。

**不允许放着不管。** 这一条比测试数量更能说明问题。

## 提 Issue

请用仓库提供的 Issue 模板（Bug 反馈 / 功能建议）。
Bug 反馈请尽量附上：**操作系统与版本、用的是网页版还是桌面端（哪个版本号）、复现步骤、期望结果与实际结果**。
课表导入相关的 bug，附上脱敏后的课表文本或截图会大幅加快定位速度 ——
**记得抹掉学号、姓名等个人信息**。

## 提 Pull Request

- 一个 PR 只做一件事。功能改动与格式化、重命名的改动请分开提。
- PR 描述里写清：**改了什么、为什么这么改、怎么验证的**。
- 涉及界面的改动，附改动前后的截图。
- 不要提交 `node_modules/`、`dist/` 产物或本地日志文件。
- 若 PR 是 AI 辅助完成的，欢迎在描述里说明 —— 本项目本身有 AI 协作开发规范
  （见 [`docs/PROMPTS.md`](docs/PROMPTS.md)），但**所有 AI 产出都必须经人工复核后才提交**，
  这跟是否使用 AI 无关，而是对代码质量负责。

## 授权

提交 PR 即表示你同意你的贡献以 [MIT 协议](LICENSE) 授权。
